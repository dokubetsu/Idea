name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: "pip"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r apps/api/requirements.txt

    - name: Run compilation check
      run: python -m compileall apps/api/app

    - name: Run pytest tests
      run: |
        export PYTHONPATH=apps/api
        pytest apps/api

  frontend-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: "npm"
        cache-dependency-path: apps/web/package-lock.json

    - name: Install npm dependencies
      run: npm --prefix apps/web ci

    - name: Run type checking
      run: npm --prefix apps/web run type-check

    - name: Run linting
      run: npm --prefix apps/web run lint

    - name: Build web application
      run: npm --prefix apps/web run build
      env:
        NEXT_PUBLIC_SUPABASE_URL: https://dsjkhphbpspawcbellal.supabase.co
        NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRzamtocGhicHNwYXdjYmVsbGFsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODEwMDg5NDIsImV4cCI6MjA5NjU4NDk0Mn0.cAxRgckRkyptcFcAeF_29fDq-_6UJI2BShiXGVUxPac
        NEXT_PUBLIC_API_URL: http://localhost:8000
